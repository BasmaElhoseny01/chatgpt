import { createTheme } from '@mui/material';
const theme = createTheme({
    palette: {
        primary: {
            main: '#10A37F',
            light: '#0E9272',
            // dark: '#1484D6'
        },
        grey: {
            main: '#E5E5E5'
        }
    }
});
export default theme;